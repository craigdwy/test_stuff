#!/usr/bin/env python

""" Test to import from the dynamic_classes module and see if the required attributes get added"""
import dynamic_classes

A = dynamic_classes.A(a=1, b=2, c=3)
X = dynamic_classes.X(x=5, y=6, z=7)
